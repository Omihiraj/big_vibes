import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFEF473);
const secondaryColor = Color(0xFF00DDF7);
const titleIconColor = Colors.blueAccent;
const titleIconColor2 = Colors.amber;
